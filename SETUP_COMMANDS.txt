═══════════════════════════════════════════════════════════════
  E-COMMERCE MARKETPLACE - SETUP COMMANDS
═══════════════════════════════════════════════════════════════

📋 STEP 1: INSTALL DEPENDENCIES
═══════════════════════════════════════════════════════════════

Terminal 1 (Backend):
  cd backend
  npm install

Terminal 2 (Frontend):
  cd frontend
  npm install

Terminal 3 (Admin):
  cd admin-frontend
  npm install
  npm install recharts


📋 STEP 2: CREATE DATABASE
═══════════════════════════════════════════════════════════════

  mysql -u root -p
  CREATE DATABASE ecommerce_marketplace;
  exit;


📋 STEP 3: CONFIGURE ENVIRONMENT
═══════════════════════════════════════════════════════════════

Create backend/.env:
  DB_HOST=localhost
  DB_USER=root
  DB_PASSWORD=your_password
  DB_NAME=ecommerce_marketplace
  JWT_SECRET=your_secret_key
  PORT=5000
  FRONTEND_URL=http://localhost:3000
  BACKEND_URL=http://localhost:5000

Create frontend/.env:
  VITE_API_URL=http://localhost:5000/api

Create admin-frontend/.env:
  VITE_API_URL=http://localhost:5000/api


📋 STEP 4: RUN MIGRATIONS
═══════════════════════════════════════════════════════════════

  cd backend
  npm run setup-features


📋 STEP 5: START SERVICES
═══════════════════════════════════════════════════════════════

Terminal 1 (Backend):
  cd backend
  npm run dev
  ✅ Running on: http://localhost:5000

Terminal 2 (Frontend):
  cd frontend
  npm run dev
  ✅ Running on: http://localhost:3000

Terminal 3 (Admin):
  cd admin-frontend
  npm run dev
  ✅ Running on: http://localhost:3001


📋 STEP 6: CREATE ADMIN USER
═══════════════════════════════════════════════════════════════

  cd backend
  node scripts/createAdmin.js


📋 STEP 7: ENABLE DASHBOARD GRAPHS (OPTIONAL)
═══════════════════════════════════════════════════════════════

  cd admin-frontend/src/pages
  mv Dashboard.jsx Dashboard.backup.jsx
  mv DashboardWithGraphs.jsx Dashboard.jsx

Then restart admin frontend (Ctrl+C and npm run dev)


═══════════════════════════════════════════════════════════════
  ✅ SETUP COMPLETE!
═══════════════════════════════════════════════════════════════

Access your application:
  🌐 Customer Site:    http://localhost:3000
  👨‍💼 Admin Dashboard:  http://localhost:3001
  🔌 API:              http://localhost:5000/api


═══════════════════════════════════════════════════════════════
  🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

Port already in use:
  # Windows
  netstat -ano | findstr :5000
  taskkill /PID <PID> /F
  
  # Linux/Mac
  lsof -ti:5000 | xargs kill -9

Database connection error:
  - Check MySQL is running
  - Verify password in backend/.env
  - Test: mysql -u root -p

Module not found:
  cd backend
  npm install
  
  cd ../admin-frontend
  npm install recharts


═══════════════════════════════════════════════════════════════
  📚 DOCUMENTATION
═══════════════════════════════════════════════════════════════

  START_HERE.md          - Quick start guide
  HOW_TO_RUN.md          - Complete setup instructions
  FINAL_ANSWER.md        - Feature overview
  QUICK_START.md         - Detailed guide
  TESTING_GUIDE.md       - Testing instructions
  DEPLOYMENT_CHECKLIST.md - Production deployment


═══════════════════════════════════════════════════════════════
  🎉 READY TO USE!
═══════════════════════════════════════════════════════════════
